node_modules/
dist/
*.local
.vite/

# Artefacts de build TypeScript
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Les assets d'origine réellement utilisés par la vitrine sont versionnés
# (repo autonome). Le dossier public/ est élagué par `npm run prune-assets`
# pour ne garder que les fichiers référencés (voir scripts/prune-assets.mjs).
#
# Exception : les médias trop lourds pour git — servis depuis le site d'origine
# (voir EXTERNAL_ASSETS dans src/lib/originalAssets.ts).
public/wp-content/uploads/**/*.mp4
